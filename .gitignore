# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
dist/
build/

# Environment files (contain secrets)
.env
.env.local
.env.*.local
backend/.env
frontend/.env
# Production secrets (JWT keys, DB password) — lives only on the deploy host
.env.production

# Credentials file (contains passwords)
.credentials

# Logs
logs/
*.log
npm-debug.log*

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Testing
coverage/

# Temp files
*.tmp
*.temp

# Prisma
backend/prisma/migrations/*_migration.sql
backend/prisma/dev.db
backend/prisma/*.db
backend/prisma/*.db-journal

secrets/

# Deployment runbook — contains the origin IP that Cloudflare proxying hides,
# plus infrastructure details of the other stack sharing the host. This repo is
# public; keep this file local.
cloud-setup-implementation.md
